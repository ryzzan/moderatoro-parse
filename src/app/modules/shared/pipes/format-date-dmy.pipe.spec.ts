import { FormatDateDmyPipe } from './format-date-dmy.pipe';

describe('FormatDateDmyPipe', () => {
  it('create an instance', () => {
    const pipe = new FormatDateDmyPipe();
    expect(pipe).toBeTruthy();
  });
});
